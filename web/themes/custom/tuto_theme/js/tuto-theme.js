/**
 * @file
 * tuto_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.tutoTheme = {
    attach (context, settings) {
      console.log('It works!');
    }
  };

} (Drupal));
